#!/bin/sh

# Run tests before commit (skip cache to avoid stale/failed cache blocking commits)
echo "Running tests before commit..."
npm test -- --skip-nx-cache

# Run lint-staged for code formatting and linting
npx lint-staged

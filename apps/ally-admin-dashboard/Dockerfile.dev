# Dev image for ally-admin-dashboard (Vite)
# Use prebuilt deps image which already has all dependencies and workspace setup
FROM ally-web/deps:dev

# Set working directory for subsequent commands
WORKDIR /app

# Configure development environment variables
ENV NODE_ENV=development

# Expose Vite dev server port
EXPOSE 8081

# Run vite directly; compose maps 8081->8081
# Start the Vite dev server via Nx
CMD ["npx", "nx", "serve", "ally-admin-dashboard", "--host=0.0.0.0", "--port=8081"]

import { ResourceSearch } from '@ally-ui-mono/ui-shared';

export default function Index() {

  return (
    <main className="w-full min-h-screen flex flex-col justify-center items-center gap-4">
      <ResourceSearch initialValue=""/>
    </main>
  );
}
